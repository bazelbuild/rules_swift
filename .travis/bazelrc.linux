# This file will be used as the bazelrc file on Linux. Add settings to it if
# you need to tweak Bazel's behavior or resource usage on that platform.

build --action_env=BAZEL_LINKOPTS=-fuse-ld=/usr/bin/ld